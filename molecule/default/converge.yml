---
- name: Converge
  hosts: all
  tasks:
    - name: "Include egdoc.jetbrains"
      include_role:
        name: "egdoc.jetbrains"
      vars:
        ide: phpstorm
        ide_version: 2022.3.1
        ide_installation_mode: system
